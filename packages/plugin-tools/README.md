# @mvmnt-app/plugin-tools

Command-line development tools for an SDK 2 MVMNT plugin project.

```sh
mvmnt-plugin check
mvmnt-plugin dev
mvmnt-plugin build
```

Commands use the current directory by default. Pass a plugin directory after the command when driving the tools
from another project. `dev` accepts multiple directories and an optional `--port`.
