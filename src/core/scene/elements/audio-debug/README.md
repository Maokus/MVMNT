# Engine-private audio fixtures

These elements exercise invalid, ad-hoc, and diagnostic audio-analysis configurations in tests.
They are not exported from the scene-element barrel, registered in the default registry, included
in plugin runtime maps, or supported as plugin authoring examples. Their class-based code and
module-local requirement setup are intentionally isolated test fixtures.
