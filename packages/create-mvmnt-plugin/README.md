# create-mvmnt-plugin

Create an external SDK 2 MVMNT plugin project:

```sh
npm create mvmnt-plugin@latest -- --name com.example.pulse --template minimal
```

Use `--dir <path>` to choose the output location. The generator never writes into the MVMNT application checkout.
